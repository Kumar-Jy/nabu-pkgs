# Maintainer: Kumar-Jy <https://github.com/Kumar-Jy>
# Full-featured on-screen touch keyboard with navigation keys, themes, and auto-popup

pkgname=oskb
pkgver=1.2.4
pkgrel=1
pkgdesc="Full-featured responsive on-screen touch keyboard with portrait/landscape auto-adapt for Linux / KDE Plasma"
arch=('any')
url="https://github.com/Kumar-Jy/nabu-pkgs"
license=('MIT')
depends=('python' 'python-gobject' 'gtk3' 'python-evdev' 'at-spi2-core' 'hicolor-icon-theme')
makedepends=('librsvg')
provides=('nabu-oskb')
install="oskb.install"
source=('oskb'
        'oskb.service'
        'oskb.desktop'
        'oskb-autostart.desktop'
        'kwinrulesrc'
        'oskb.svg'
        '99-uinput-oskb.rules'
        'oskb-accessibility.sh'
        'LICENSE'
        'README.md')
sha256sums=('SKIP'
            'SKIP'
            'SKIP'
            'SKIP'
            'SKIP'
            'SKIP'
            'SKIP'
            'SKIP'
            'SKIP'
            'SKIP')

package() {
  install -Dm755 "$srcdir/oskb" "$pkgdir/usr/bin/oskb"
  ln -sf oskb "$pkgdir/usr/bin/nabu-oskb"
  install -Dm644 "$srcdir/oskb.service" "$pkgdir/usr/lib/systemd/user/oskb.service"
  install -Dm644 "$srcdir/oskb.desktop" "$pkgdir/usr/share/applications/oskb.desktop"
  install -Dm644 "$srcdir/oskb-autostart.desktop" "$pkgdir/etc/xdg/autostart/oskb.desktop"
  install -Dm644 "$srcdir/kwinrulesrc" "$pkgdir/etc/xdg/kwinrulesrc"

  # Scalable and pixmap icons
  install -Dm644 "$srcdir/oskb.svg" "$pkgdir/usr/share/icons/hicolor/scalable/apps/oskb.svg"
  install -Dm644 "$srcdir/oskb.svg" "$pkgdir/usr/share/pixmaps/oskb.svg"

  # KDE Breeze & Breeze-Dark icons
  install -Dm644 "$srcdir/oskb.svg" "$pkgdir/usr/share/icons/breeze/apps/48/oskb.svg"
  install -Dm644 "$srcdir/oskb.svg" "$pkgdir/usr/share/icons/breeze-dark/apps/48/oskb.svg"

  # Standard raster sizes for hicolor
  for sz in 16 22 24 32 48 64 128 256; do
    rsvg-convert -w "$sz" -h "$sz" "$srcdir/oskb.svg" -o "$srcdir/oskb-${sz}.png"
    install -Dm644 "$srcdir/oskb-${sz}.png" "$pkgdir/usr/share/icons/hicolor/${sz}x${sz}/apps/oskb.png"
  done

  install -Dm644 "$srcdir/99-uinput-oskb.rules" "$pkgdir/usr/lib/udev/rules.d/99-uinput-oskb.rules"
  install -Dm644 "$srcdir/oskb-accessibility.sh" "$pkgdir/etc/profile.d/oskb-accessibility.sh"
  install -Dm644 "$srcdir/LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  install -Dm644 "$srcdir/README.md" "$pkgdir/usr/share/doc/$pkgname/README.md"
}
