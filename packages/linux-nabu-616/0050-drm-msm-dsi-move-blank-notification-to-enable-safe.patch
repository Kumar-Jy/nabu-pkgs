From 8a8827801f0d90b3c3a72fe24f9a1b24c3d5e9a1 Mon Sep 17 00:00:00 2001
From: Kumar-Jy <20044626+Kumar-Jy@users.noreply.github.com>
Date: Sun, 31 Aug 2026 10:30:00 +0530
Subject: [PATCH 50/54] drm: msm: dsi: move blank-notify to enable and defer on
 a workqueue

The NT36523 touchscreen resumes on MI_DRM_EVENT_BLANK / MI_DRM_BLANK_UNBLANK.
It was previously emitted from dsi_mgr_bridge_pre_enable(), before the DSI
host is actually streaming, so after suspend the touch controller would not
reliably wake back up against a live panel.

Move the emit into dsi_mgr_bridge_enable() so the touch resumes while the
panel is live. Enable runs inside the kms atomic commit while its locks are
held, and the blocking notifier chain's subscriber (nt36523) waits on a
driver workqueue that needs the very locks the commit holds, which would
deadlock (seen as a switch-root hang). Defer the notification onto a system
workqueue so it runs only after the commit has released its locks,
preserving the enable-time post-stream timing that makes wakeup work without
the deadlock.

Signed-off-by: Kumar-Jy <20044626+Kumar-Jy@users.noreply.github.com>
---
 drivers/gpu/drm/msm/dsi/dsi_manager.c | 31 ++++++++++++++++++++++++++-
 1 file changed, 30 insertions(+), 1 deletion(-)

diff --git a/drivers/gpu/drm/msm/dsi/dsi_manager.c b/drivers/gpu/drm/msm/dsi/dsi_manager.c
--- a/drivers/gpu/drm/msm/dsi/dsi_manager.c	2026-08-31 10:57:46.905960225 +0530
+++ b/drivers/gpu/drm/msm/dsi/dsi_manager.c	2026-08-31 10:57:46.907875660 +0530
@@ -8,6 +8,7 @@
 #include "msm_kms.h"
 #include "dsi.h"
 #include "drm/drm_notifier.h"
+#include <linux/workqueue.h>
 
 #define DSI_CLOCK_MASTER	DSI_0
 #define DSI_CLOCK_SLAVE		DSI_1
@@ -276,6 +277,33 @@
 	dsi_mgr_phy_disable(id);
 }
 
+/* blank-notify deferral work (see dsi_mgr_bridge_enable below) */
+static struct work_struct dsi_blank_work;
+static bool dsi_blank_work_inited;
+
+static void dsi_blank_work_fn(struct work_struct *work)
+{
+	enum drm_notifier_data blank = MI_DRM_BLANK_UNBLANK;
+
+	mi_drm_notifier_call_chain(MI_DRM_EVENT_BLANK, &blank);
+}
+
+static void dsi_mgr_bridge_enable(struct drm_bridge *bridge)
+{
+	/*
+	 * Enable runs inside the kms atomic commit while its locks are held.
+	 * The MI blank notifier chain is blocking and its nt36523 subscriber
+	 * waits on a driver workqueue, so calling it synchronously here would
+	 * deadlock while the commit holds the very lock that work item needs.
+	 * Defer to a system worker so it runs after the commit releases them.
+	 */
+	if (!dsi_blank_work_inited) {
+		INIT_WORK(&dsi_blank_work, dsi_blank_work_fn);
+		dsi_blank_work_inited = true;
+	}
+	schedule_work(&dsi_blank_work);
+}
+
 static void dsi_mgr_bridge_pre_enable(struct drm_bridge *bridge)
 {
 	int id = dsi_mgr_bridge_get_id(bridge);
@@ -284,7 +312,6 @@
 	struct mipi_dsi_host *host = msm_dsi->host;
 	bool is_bonded_dsi = IS_BONDED_DSI();
 	int ret;
-	enum drm_notifier_data notifier_data;
 
 	DBG("id=%d", id);
 
@@ -298,9 +325,6 @@
 		return;
 	}
 
-	notifier_data = MI_DRM_BLANK_UNBLANK;
-	mi_drm_notifier_call_chain(MI_DRM_EVENT_BLANK, &notifier_data);
-
 	ret = msm_dsi_host_enable(host);
 	if (ret) {
 		pr_err("%s: enable host %d failed, %d\n", __func__, id, ret);
@@ -456,6 +480,7 @@
 static const struct drm_bridge_funcs dsi_mgr_bridge_funcs = {
 	.attach = dsi_mgr_bridge_attach,
 	.pre_enable = dsi_mgr_bridge_pre_enable,
+	.enable = dsi_mgr_bridge_enable,
 	.post_disable = dsi_mgr_bridge_post_disable,
 	.mode_set = dsi_mgr_bridge_mode_set,
 	.mode_valid = dsi_mgr_bridge_mode_valid,
-- 
2.49.0
